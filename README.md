# xsec

## under construction

slender beam cross-sectional analysis

## usage

first time, init venv:
`python -m venv venv`

first time, install dependencies:
`pip install -e .[dev]`

every time, activate venv:
`source venv/bin/activate`

if interested, run test cases:
`pytest -v`
